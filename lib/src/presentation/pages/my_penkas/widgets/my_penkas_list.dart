import 'package:flutter/material.dart';

class MyPenkasList extends StatelessWidget {
  const MyPenkasList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'MyPenkasList is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
